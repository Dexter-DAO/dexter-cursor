node_modules/
dist/
.DS_Store
*.log
.env
.env.local
# The root workspace lock is release-controlled. Per-workspace locks are
# forbidden because they can silently disagree with the graph npm ci uses.
/packages/*/package-lock.json
